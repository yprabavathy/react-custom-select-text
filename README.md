# react-custom-select-text

A React component that provides a **multi-select dropdown** with checkboxes and an input field for each selected option.

## Installation

```bash
npm install react-custom-select-text
```
